import 'package:flutter/material.dart';

class AppColors {

  // Uygulamanın ana mor rengi
  static const Color primary = Color(0xFF6C63FF);

  // Vurgu ve ikincil işlemler için
  static const Color secondary = Color(0xFF03DAC6);

  // Genel sayfa arka planı
  static const Color background = Color(0xFFF9F9F9);

  // Kartların ve kutucukların arka planı
  static const Color surface = Colors.white;

  // Ana başlıklar için koyu renk
  static const Color textPrimary = Color(0xFF2D2D2D);

  // Açıklamalar ve alt başlıklar için
  static const Color textSecondary = Color(0xFF757575);

  // Hata mesajları için
  static const Color error = Color(0xFFB00020);

  // Başarılı işlemler için
  static const Color success = Color(0xFF4CAF50);

  // Kenarlıklar ve çizgiler için
  static const Color border = Color(0xFFE0E0E0);
}